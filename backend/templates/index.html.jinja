<!DOCTYPE html>
<html lang="de">
<head>
    <title>{{ root_name | e }} Who-Is-Who</title>
    <link rel="stylesheet" type="text/css" href="styles.css">
</head>
<body>
  <div class="container">
    <a href=/api/html/{{ locale }}/{{ root_id }}><h1>{{ root_name | e }}</h1></a>
    <h2 class="group-name">{{ group.name[locale] | e }}</h2>
    {% if subgroups %}
      {% include "_group.html.jinja" %}
    {% else %}
      {% include "_group_detail.html.jinja" %}
    {% endif %}
  </div>
</body>
</html>
